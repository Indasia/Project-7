import React from 'react';
import Form from './Form';
import Nav from './Nav';

const Home = props => {
    return (
        <div className="main-content">
            <h1>React Image Finder</h1>
            <p>Search for an image in the search bar above</p>
        </div>
    );
}

export default Home;